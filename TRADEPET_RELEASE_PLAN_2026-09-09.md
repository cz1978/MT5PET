# TradePet 1.0 正式版统一修改与发布计划

编写日期：2026-09-09

目标版本：`1.0.0`，验收期间使用 `1.0.0-rc.N`

当前状态：**计划已编写，以下正式版工作尚未按本计划完成验收。**

这是一份完整版本的实施契约：修复已确认的数据与交互缺陷，补齐产品流程、架构、测试、安装升级和支持能力，最后对同一提交、同一发布包统一验收。工作编号表示依赖顺序，不代表分批交付或允许把中间产物称为正式版。

本计划承接 [原复盘统一方案](TRADEPET_REVIEW_UPGRADE_PLAN_2026-09-06.md) 的 R01–R12、V01–V20。已有功能保留并回归；新发现的问题以本计划的要求为准。执行进度只在 [TODO.md](TODO.md) 维护，本文维护规格、验收标准及变更理由。

## 1. 正式版的范围与完成定义

### 1.1 产品承诺

| 项目 | 1.0 的交付范围 |
| --- | --- |
| 产品定位 | 本地优先的 Windows MT5 只读交易桌宠、交易日志和复盘工具。 |
| 平台 | 正式支持实际验收过且仍获厂商支持的 Windows 11 x64 版本；发布清单记录准确 OS build。Windows 10、ARM64、macOS 不宣称正式支持。 |
| MT5 | 去除 WeTrade 路径依赖；至少验证两种券商终端分发、多终端共存、同一 position ID 跨账户隔离。完整交易投影支持对冲账户。 |
| 其他账户模式 | 净额与交易所账户提供明确的持仓/账户监控；复盘入口说明限制。其完整投影涉及交易身份重新定义，列为后续范围，不以对冲算法冒充支持。 |
| 离线使用 | MT5 未启动或网络断开时，已同步交易、日记、策略、搜索、备份与本地回放仍可使用。 |
| 复盘 | 保留六个入口及既有功能；补齐草稿保护、图表批注、全局检索、导入与报告完整性、指标解释。 |
| 数据迁移 | 支持当前本地库、原 schema 1–8 的有效历史样本及原格式完整备份迁入；不默默另建空库。 |
| 外部数据 | 提供通用 CSV 导入及映射预览；导入数据独立标识来源，不混入 MT5 实时会话、提醒或无法证明的过程统计。 |
| 发布 | 签名安装包、完整离线运行时、版本和依赖清单、更新检查、失败恢复、诊断包、用户手册。 |
| 隐私 | 默认本地处理；公开导出按明确范围脱敏；加密工作区和加密备份可用；诊断不自动上传。 |

云同步、手机端、教练协作、付费订阅、自动交易和独立商业行情供应商不属于 1.0 的发布承诺。它们是产品范围选择，不是可以跳过数据安全或可靠性验收的理由。完整净额账户复盘也须另立真实成交样本和身份规范，不能在本次验收末尾临时塞入。

### 1.2 统一完成条件

- WP01–WP28 全部有实施提交和相应验收证据。
- 本文 G01–G36 及旧 V01–V20 均通过；旧报告只作历史证据，不能代替新版验收。
- 没有未解决的 S0/S1 缺陷，也没有影响本次承诺主流程的 S2 缺陷。
- 外部发布条件 E01–E05 已满足；没有证书、分发地址或桌面验收时，只能称为“候选版”或“待发布”，不能称为已发布。
- 安装、升级、恢复、签名和性能测试针对最终候选包；任何业务代码、运行时、原生库或包配置改变，重新执行受影响门槛。

严重性：S0 为跨账户污染、不可逆数据损失、敏感数据泄露或交易写操作；S1 为启动/保存/恢复/升级主流程失效或重要统计错误；S2 为局部功能错误、明显性能或可访问性缺陷；S3 为不妨碍使用的文案或外观问题。这里只定义分类，不假定所有路径已在真实环境复现。

## 2. 已有基础与必须纠正的问题

已有 Core / Application / Infrastructure / WPF 分层、账户复合身份、乐观修订、后台任务重启、有界消息队列、备份哈希校验、分页筛选、保存筛选、小样本提示及只读行情回放。不要重复开发，也不能因为“已有”就跳过边界测试。

截至本次方案编写，TODO 的历史记录为 201 项 .NET 测试和 16 项 Python 测试通过；这不是本次正式验收结果。二次审查实际重跑了 1 项导出测试及 2 项备份测试，均通过，但测试缺少本次发现的真实行为 ID 和切换中途失败情形。

| 缺陷 | 代码证据与影响 | 对应工作 |
| --- | --- | --- |
| F01 账户切换保留编辑状态 | [切换逻辑](src/TradePet.App/Runtime/TradePetRuntime.cs:810) 只清列表；[保存逻辑](src/TradePet.App/Runtime/TradePetRuntime.cs:2537) 使用当前账户，可能把旧草稿写入新账户。 | WP02、WP03 |
| F02 保存回执误清新修改 | [MarkReviewSaved](src/TradePet.App/ViewModels/Review/ReviewWorkspaceViewModel.cs:867) 无条件取消待保存任务并清除 dirty。保存中继续输入时，最新文本可能未落库。 | WP04 |
| F03 日记/周期草稿覆盖、退出未统一落盘 | [周期表单回填](src/TradePet.App/ViewModels/Review/ReviewWorkspaceViewModel.cs:680)、[日记切换](src/TradePet.App/Runtime/TradePetRuntime.cs:2934)、[退出](src/TradePet.App/Runtime/TradePetRuntime.cs:580)。 | WP04、WP11 |
| F04 恢复回滚间隙 | [文件切换](src/TradePet.Infrastructure/Persistence/ReviewArtifactStore.cs:369) 先移走旧数据、后置切换标记，finally 清理旧副本；中途失败可能使原数据未还原。 | WP07 |
| F05 公开导出账号泄露 | [行为 ID](src/TradePet.Core/Trading/BehaviorAnalyticsCalculator.cs:361) 包含账户，随后被[原样导出](src/TradePet.Application/Review/ReviewServices.cs:1208)。 | WP06 |
| F06 导出范围不一致 | [导出入口](src/TradePet.App/Runtime/TradePetRuntime.cs:3645) 遍历分页结果；[机会附件查询](src/TradePet.Infrastructure/Persistence/AppDatabase.ReviewWorkspace.cs:91) 只按账户。 | WP05、WP06 |
| F07 读取非一致快照 | [LoadWorkspaceAsync](src/TradePet.Infrastructure/Persistence/AppDatabase.ReviewWorkspace.cs:25) 跨连接、多次查询，可能把不同版本缓存为一个版本。 | WP05 |
| F08 单笔详情全量扫描 | [LoadTradeDetailAsync](src/TradePet.Infrastructure/Persistence/AppDatabase.ReviewWorkspace.cs:113) 加载全账户后内存筛选，导出逐笔重复。 | WP05、WP24 |
| F09 损坏库重建不能恢复人工资料 | [初始化恢复](src/TradePet.Infrastructure/Persistence/AppDatabaseRecovery.cs:18) 留档重建后依赖 MT5 回填，人工笔记、图片关联和策略无法由 MT5 重建。 | WP07、WP08、WP10 |
| F10 可交付性不足 | Git 尚无提交；[发布脚本](scripts/build-release.ps1) 仅 publish；[Bridge 编译](scripts/build-bridge.ps1) 只检查文件存在，可能误用旧 EX5；[运行时定位](src/TradePet.App/Runtime/RuntimePaths.cs) 依赖本机 Python。 | WP01、WP20–WP23 |
| F11 交易频率提醒口径与文案不一致 | [触发逻辑](src/TradePet.Core/Trading/BehaviorAnalyticsCalculator.cs:337) 可因绝对笔数越线，即使当前值低于个人基线；[基线计算](src/TradePet.Core/Trading/BehaviorAnalyticsCalculator.cs:440) 实际取全历史日峰值排序后的较高 20 项；[气泡文案](src/TradePet.App/Runtime/TradePetRuntime.cs:1539) 却一律写“平时才”，会出现“7 笔、平时 10 笔”仍提醒且原因不明。 | WP17 |

以上是已识别路径，不是“证明不存在其他问题”。WP26 要独立检查剩余边界并把新增缺陷纳入同一版本。

## 3. 文件影响图与目标架构

下表新增目录是拟建结构，实施时允许按现有命名作小幅调整，但责任边界和验收不能减少。

| 文件或目录 | 角色 | 计划修改 |
| --- | --- | --- |
| `src/TradePet.Core/Domain/` | 身份、修订、事实模型 | 编辑身份、导出范围、恢复状态、导入来源、批注与支持能力契约。 |
| `src/TradePet.Core/Session/`、`Trading/`、`Review/` | 纯状态机与计算 | 会话代次、交易和时间正确性、统计边界、回放可见性。 |
| `src/TradePet.Application/Review/` | 复盘用例 | 分离查询、编辑、导出、回放、目标服务，所有命令显式携带账户身份。 |
| `src/TradePet.Application/Editing/`、`Recovery/`、`Import/`（新增） | 跨 UI 的业务协调 | 草稿提交、恢复状态机、CSV 预览和幂等导入。 |
| `src/TradePet.Infrastructure/Persistence/` | SQLite、文件、加密 | 连接工厂、一致读、事务写、迁移、备份、故障注入边界。 |
| `src/TradePet.Infrastructure/Mt5/`、`python/`、`mt5/` | 外部适配 | 协议能力协商、进程生命周期、只读接入、构建可追溯。 |
| `src/TradePet.App/Runtime/TradePetRuntime.cs` | 应用编排 | 逐步迁出账户、采集、规则、复盘、恢复与生命周期职责；不再承载具体 SQL/打包逻辑。 |
| `src/TradePet.App/App.xaml.cs`、`Runtime/AppLog.cs` | 启动/退出、错误 | 全局错误兜底、退出协调、脱敏日志和诊断。 |
| `src/TradePet.App/ViewModels/Review/`、`Views/`、`Controls/` | 编辑状态与 WPF | 独立编辑器、保存状态、范围预览、首次引导、图表批注、可访问性。 |
| `src/TradePet.Infrastructure/Diagnostics/`、`Security/`（新增） | 本地支持与密钥 | 健康状态、诊断打包、密钥包装、附件加密和数据清理。 |
| `tests/`、`tools/TradePet.ReviewBenchmark/` | 自动验证 | 补足并发、文件故障、进程集成、导出与性能验证。 |
| `tests/TradePet.App.Tests/`、`TradePet.Ui.Tests/`（新增） | 编辑器与原生 UI 验证 | 可控调度测试和交互式 Windows 桌面测试。 |
| `scripts/`、`packaging/`、`.github/workflows/`（新增） | 构建和分发 | SDK/依赖锁定、离线 Python、签名 MSIX、更新索引、发布门禁。 |
| `docs/`、`README.md`、`CHANGELOG.md` 等 | 产品与支持文档 | 用户手册、指标字典、支持矩阵、恢复操作、许可证及发布证据。 |

依赖方向保持 `App → Application → Core`，`Infrastructure → Application/Core`；App 组合根装配基础设施。Core 不依赖 WPF、SQLite 或 MT5。把大文件拆成 partial 但仍共享全部可变状态，不算完成架构改造。

## 4. 不变量与共同 API 契约

1. **只读交易**：订单写接口不存在于应用能力白名单；测试通过 mock、历史录制和隔离账户完成，不用真实下单制造场景。
2. **身份完整**：交易编辑身份为 `AccountKey + PositionId + SessionGeneration + EditorInstanceId`；日记/机会/策略等同样携带账户与实体 ID。会话切换后旧读结果和保存回执不能修改新编辑器。
3. **草稿真实性**：保存请求携带不可变内容快照、`ExpectedRevision` 和 `EditSequence`。只有回执覆盖当前内容序号时才可清 dirty；冲突/取消/失败不能显示成功。
4. **事实与编辑分离**：MT5 成交不可人工覆盖；导入、盘前计划、盘中观测、事后记录来源及实际记录时间独立保存。更正规则触发重审并保留原结论。
5. **读取一致性**：一次查询的交易、成交、文档、指标和版本来自同一数据库读快照；导出固定相同的账户、过滤、版本、实体清单和附件清单。
6. **写入原子性**：业务修改、审计修订和版本递增在同一事务。附件先暂存、校验再建立关联；失败的暂存文件可回收，不删除仍被引用的资产。
7. **维护互斥**：恢复/迁移/加密转换/账户删除前停止接受新写入，等待所有数据库使用者退出，包括查询、导出、回放和后台写入。使用单一维护协调入口，避免嵌套锁形成反向等待。
8. **恢复可续行**：状态落盘后再切换；发生异常或进程中断，重启可辨认“旧集”“新集”和校验结果。新集未验证不得清理最后一份有效旧集。
9. **未知不造值**：无初始风险、无报价转换、采样不足、行情缺口和账户模式不支持都保留缺失状态；不能补零、猜汇率或伪造策略优势。
10. **脱敏按允许字段输出**：公开模型不直接序列化领域实体；跨文件引用采用导出包内临时 ID。不能仅删除名为 AccountKey 的字段。
11. **生命周期可等待**：业务命令返回 Task，异步入口由统一协调器观察；停止、取消、超时和崩溃状态明确，不能通过吞异常宣称运行正常。
12. **外部可验证**：源码提交、应用版本、Bridge、Python、原生库、schema、协议和导出格式版本可追溯。旧程序遇到新 schema 必须拒绝写入。

## 5. 实施工作包

每项执行规则：先在 TODO 激活一个工作包；阅读调用者、类型和相邻测试；先写能暴露问题的失败场景；按类型/契约 → 核心逻辑 → 适配 → UI → 测试/文档修改；定向验收通过再进入下一项。涉及已有功能重构时保留行为回归，不能同时更换统计口径。

工作包后面的 G 编号是验收需求映射。单项完成时验证其负责的子场景；跨工作包的完整 G 项在 WP28 汇总验收，不能因为先完成其中一部分就勾选整项，也不要求当前工作包等待尚未实施的后续功能。

### WP01 建立可追溯基线

- 范围：`.gitignore`、版本文件、仓库根、发布样本清单。
- 清点并排除实盘数据库、截图、账户日志、证书、密钥和机器路径配置；逐项暂存已审查文件，建立初始提交和基线标签。Git 作者身份缺失时记录外部条件，不虚构身份。
- 留存当前可运行包的哈希、schema/依赖版本和脱敏样本；对现有数据做一致性备份后再进行后续实际迁移。
- 交付：可回退的源码基线、初始缺陷台账、版本命名规范。远端推送或公开不包含在“建立本地基线”的自动动作内。
- 依赖：无。验收：G01。

### WP02 建立编辑与维护契约、可注入测试入口

- 范围：Core 共享类型、Application 接口、Runtime 组合根、新 App 测试项目。
- 增加完整编辑身份、内容序号、保存结果、会话取消、维护租约；给时钟、调度、仓储和文件操作提供可替换接口。
- App 测试能暂停任意 await、控制账户切换和回执顺序，不需要启动真实 MT5。类型变化同一项内更新全部调用者。
- 依赖：WP01。验收：G02、G03。

### WP03 修复跨账户与过期响应

- 切换前按旧身份保留草稿；取消旧定时器和请求，提升会话代次，清除所有账户编辑器及选中 ID。保存从编辑器身份读取账户，禁止临时拼入当前账户。
- 保存层检查目标交易/实体存在和来源匹配；拒绝不存在交易的孤儿文档。旧会话已提交的保存可落在旧账户，但回执不得应用到新账户 UI。
- 详情 A→B→A 的乱序请求、快速重复点击、切换到不支持模式、导入账户与实时账户混用全部设测试。
- 依赖：WP02。验收：G03、G04。

### WP04 统一所有编辑器的保存与导航保护

- 交易、日记、周期总结、规则评价、机会、目标和批注采用一致 dirty/save/error 状态；按实体保存独立草稿，不让刷新覆盖未提交输入。
- 修复“保存中继续输入”：只确认已提交序号，保留新 dirty 并继续排队；冲突提供重载、保留本地副本和人工合并，不静默覆盖。
- 普通切换能先保存则保存；失败停留或显式保留草稿。退出先停止新编辑、提交队列，超时或不可写提供保留/导出草稿与退出选择。
- 自动草稿目标：停止输入后 1 秒内进入提交；正常磁盘条件下 2 秒内明确结果。崩溃恢复只承诺已完成的持久化草稿，不声称每个按键都能抵抗瞬时断电。
- 依赖：WP03。验收：G05、G06。

### WP05 统一读快照并修正查询成本

- 引入明确的一致读事务，所有相关查询使用同一连接和事务；长导出使用隔离快照，避免长期占用生产 WAL。
- 按账户和 position 精确查询详情/成交；批量加载导出详情，避免 N 次全账户扫描。过滤字段增加必要索引，记录实际查询计划。
- 分离 `PagedTrades`、全部筛选结果及聚合数据，导出不能借用分页字段。机会附件按已选机会 ID 关联查询。
- 缓存键包含全部影响结果的版本/过滤参数；连接期间数据变化不得把旧内容记到新版本名下。
- 依赖：WP02–WP04。验收：G07、G08、G09。

### WP06 修正公开导出、范围与完整性

- 明确“全部筛选结果”“当前选中记录”的导出范围，预览交易/机会/附件数量和日期；默认全部筛选结果，不能悄悄只导出当前页。
- 公开输出重新映射行为、计划、交易等内部 ID，清理账户号、服务器名、机器路径及嵌套证据引用；保留允许公开的统计字段。CSV 防公式注入覆盖空白、制表符/换行前缀等边界；HTML 所有用户文本转义。
- 自由文本、截图和文件名可能含账号：公开导出默认不带原始附件，提供逐项选择、脱敏批注及预览；不对用户主动选入的图片宣称自动完全匿名。
- CSV/JSON/HTML/Markdown 使用同一冻结数据集，数量、盈亏、修订和清单互相一致；附件失踪/哈希变化必须失败或经明确选择生成带缺失标记的包，不能默默跳过。
- 流式打包、取消清理临时产物、长 ID 以字符串保存；公开包与完整本地备份用途和界面分开。
- 依赖：WP05。验收：G09、G10、G11。

### WP07 修复恢复状态机和文件切换原子边界

- 使用统一维护协调器停止数据库读写，等待活动租约释放，可靠处理 WAL/SHM；不能靠 `ClearAllPools` 代替停止活动连接。
- 验证来源包 → 创建当前一致备份 → 持久化恢复操作记录 → 暂存新集 → 逐步切换 → 初始化校验 → 提交状态 → 可延期清理旧集。
- 每一步记录可恢复状态；旧库/旧附件的移动分别记录，不把两个动作完成后的布尔值当作唯一回滚依据。失败和取消后先还原并验证，失败文案按实际结果输出。
- 进程被结束后能按状态记录续行或回滚；所有测试在临时工作区注入磁盘满、拒绝访问、文件占用、第二次移动失败和校验失败。
- 依赖：WP02、WP04–WP06。验收：G12、G13。

### WP08 迁移兼容与损坏数据抢救

- 加入应用最小/最大可读写 schema 检查、迁移校验和、独占锁、迁移前备份；失败不得半迁移运行。
- 损坏检测首先区分错误密钥、格式不支持、权限/磁盘故障与真实损坏。恢复工具在副本上读取有效表/记录，生成抢救数量、失败表、未恢复附件和受影响实体报告。
- 优先恢复最近验证备份及可读人工资料；只有明确告知人工资料无法回填后才允许创建新工作区；原库和侧文件保持可追溯。
- 支持 schema 1–8 及新格式样本、重复启动、任意迁移中断。回退程序不等于回退数据：保留升级后新增记录，兼容性不足时提供恢复副本或前向修复。
- 已分发的旧便携程序无法被新代码追溯加上 schema 防护：迁入正式版时采用独立、已登记的新工作区位置并保留原路径的旧库副本，不能让无保护的旧程序继续打开新库。旧/新数据的归属、备份和回迁必须在引导中说明。
- 依赖：WP07。验收：G14、G15。

### WP09 工作区加密与密钥恢复

- 数据库采用 SQLCipher 兼容的维护中原生构建，通过 `Microsoft.Data.Sqlite.Core`/连接工厂统一接入；默认工程路线为固定源码和构建环境构建、校验及更新，不依赖已弃用的免费 bundle。采购受支持二进制是可替代决策，不自动购买。
- 新工作区使用随机数据密钥，Windows DPAPI 包装本机密钥；提供可迁移的恢复密钥/口令包装。附件使用成熟库的认证加密，文件名和临时文件也纳入保护。
- 数据库、WAL、临时查询文件和附件均检查是否残留明文；错误密钥进入解锁/恢复流程，绝不能被判为损坏后重建空库。
- 原明文工作区通过已验证备份、暂存转换和切换迁入。遗留备份清单单独提示；SSD/系统备份不能承诺物理安全擦除。
- 加密备份使用维护中且有认证的容器实现，固定格式/KDF 参数并校验篡改；不自行设计密码算法。DPAPI 本机备份不能替代跨机恢复包。
- 依赖：WP08。验收：G16、G17。

### WP10 自动备份、保留与恢复演练

- 手动备份、每天首次有效使用后的自动备份、升级/恢复/大批量导入/删除前备份；运行时检查到期，避免为首次版本新增常驻系统服务。
- 默认保留最近 7 个日备份、4 个周备份、3 个月备份；仅对通过校验的新备份执行轮换，绝不删最后一份有效备份。跨盘目标可配置，本机同盘不称为灾难恢复。
- 显示最后成功时间、失败原因、大小、加密方式和目标路径；备份不足磁盘空间时给出可执行提示。
- 每次候选发布在独立工作区恢复并对人工文档、策略、引用与附件哈希比对；明确数据恢复点与预期恢复时间。
- 依赖：WP09。验收：G17、G18。

### WP11 启动、退出、崩溃与诊断

- 在 WPF 启动最早处建立日志与全局异常边界；区分可恢复操作异常与必须退出的致命错误，不能把所有异常标成 Handled 后继续写数据。
- 安全模式可禁用采集/复杂视图并导出诊断；正常退出等待 WP04 草稿和受监督后台任务，处理会话注销、系统关机和重复启动。
- 诊断包含版本、事件 ID、错误栈、匿名化会话 ID、健康状态、schema 和备份状态；默认不含数据库、正文、截图或真实账户，预览后由用户自行提供。
- 启动失败仍能写最小故障记录，日志自身不可写时给备用路径提示；日志分级、限额和轮换，处理重复异常刷屏。
- 依赖：WP04、WP10。验收：G19、G20。

### WP12 采集进程、协议与实时恢复

- 保留现有后台监督和有界队列，补退避、连续失败健康状态、心跳超时、帧大小/数量限制、非法数据拒绝和协议能力版本。
- 会话切换、终端关闭/升级、Python 被结束、Bridge 重挂、睡眠唤醒、同用户多个终端连接不能生成重复事实或旧提醒。
- 历史请求从进程启动开始即纳入超时和 finally 清理；任何路径取消后不留下 Python 子进程；重放不写实时状态。
- 完整采集后的状态清晰区分已连接、可用但数据过期、补齐中、故障、模式不支持；遇到缺块只能记未完成，不错误推进同步水位。
- 依赖：WP02、WP11。验收：G21、G22。

### WP13 交易账务、时间与风险口径验收

- 固定脱敏金标准：部分平仓、加仓、隔夜、重复/更正成交、手续费后补、入出金、信用调整、成交类型变化和多个账户同 ID。
- 对成交现金账、完整交易盈亏、净值回撤分别核对；金额以数据源精度/币种小数位定义容差，不能统一用 0.01 忽略非两位币种。
- 历史时区使用分段时间规则，覆盖 DST、服务器时间调整、PC 时间变化和跨日；不把当前 UTC 偏移套用全部历史。
- 初始风险基于当时可证明的止损与换算，明确滑点/费用/采样覆盖；重复同步、升级、重建投影后总账不变。
- 依赖：WP05、WP12。验收：G23、G24。

### WP14 真实回放与批注

- 回放请求/画面携带账户、交易、请求和会话身份；旧行情返回不能覆盖新选中交易。
- 验证没有未来信息泄漏，尤其 K 线在尚未收盘时不能显示完整最终 OHLC；只有 bars 时明确采用已收盘 bar 步进或事件模式，不伪装 tick 精度。
- 图表支持交易点、止损观测、价位/时间锚定文字和线段批注；显示创建/修改时间及事后标记，批注可编辑、删除和撤销。
- 缺口、周末、终端无历史、同毫秒多 tick、费用晚到及不同价格精度均正确降级；保留行情请求来源与版本，支持按需重试补齐。
- 依赖：WP04、WP05、WP12、WP13。验收：G25、G26。

### WP15 通用 CSV 导入与重复/更正处理

- 新增导入向导：选择来源和独立账户、编码/分隔符、时区/币种、字段映射、单位和交易类型；支持保存映射模板。
- 区分成交级数据和完整交易摘要。摘要不能生成并不存在的成交过程、MAE/MFE 或初始 R；独立账户使用来源命名空间，不能依文件中一个登录号合并实时 MT5 账户。
- 正式写入前展示行数、错误行、日期范围、费用和盈亏核对；没有币种/时间语义的行必须补全或拒绝。
- 用来源账户＋来源记录 ID/稳定指纹幂等导入；同 ID 不同内容进入更正预览。保存导入批次和源摘要，撤销只回滚该批数据，保留后来人工资料并报告失去交易关联的文档。
- 依赖：WP04、WP05、WP10、WP13。验收：G27。

### WP16 补齐新用户、离线和复盘使用流程

- 首次引导验证数据目录、加密恢复方式、终端选择、账户模式、Bridge 挂图和历史补齐状态；失败可重试，离线也能进入已有工作区。
- 演示数据通过独立演示工作区提供，明显标识，可一键退出；演示操作不接触真实数据或触发实盘提醒。
- 账户选择和工作区身份始终可见；没有 MT5 连接时，从本地账户目录载入复盘，不能要求 `_account` 实时快照非空才允许编辑。
- 实现跨交易正文、日记、机会与策略的账户内全局搜索；结果高亮、清楚的范围和跳转，保存筛选继续沿用现有实现。
- 统一空、加载、无权限、保存失败、缺数据和需重审状态；错误提示给出重试/查看详情路径。
- 日/周/月总结提供可编辑模板，报告预览保留完整事实、执行质量、改进动作及证据链接。
- 依赖：WP03、WP04、WP11、WP14、WP15。验收：G06、G28。

### WP17 指标字典与统计可信度

- 每个重要指标提供公式、分母、币种/日期口径、样本范围和缺失原因；沿用已存在的小样本提示，统一规则和文案。
- 交易频率基线按明确的历史日期窗口计算，区分“相对个人基线”和“绝对笔数”触发；提醒必须展示本次实际越过的门槛，不在当前值低于基线时使用“平时才”文案。
- 胜率等比例附不确定性区间，盈亏均值/策略对照显示样本数量和偏斜影响；方法由纯计算测试和数值参考样本验证，不显示无依据“显著优于”。
- 同账户频繁相关交易不直接假设独立样本；未校验相关性和多重比较时，只提供描述性对照，不提供误导性优势评分。
- 行为相关损益保持去重；基线和改进目标版本冻结，未知规则不当作通过，未出现触发机会不当作改善。
- 时间、盈亏单位、CSV 与 UI 使用同一口径字典，原方案 V05–V13 逐项复验。
- 依赖：WP13、WP16。验收：G23、G29。

### WP18 数据管理、附件安全与审计

- 附件校验扩展名与真实格式、像素尺寸、文件/数量上限和哈希；禁止路径穿越、重解析点越界和默认执行任意导入文件。
- 资产引用计数与事务一致；孤儿扫描先给预览，保留正在导入/恢复/导出的对象，清理只操作受控目录。
- 提供账户数据导出/删除、导入批次撤销、日志/缓存清理和存储占用；删除需明确账户与关联数量并提供最后备份。
- 删除账户同时覆盖派生数据、搜索索引、附件链接、自动草稿和缓存；说明历史外部备份需另行管理，禁止把物理安全擦除作为保证。
- 为人工编辑、批量修改、导入、更正、恢复和删除保存可读的本地操作记录；不重复保存无用途的敏感日志。提供适用操作的撤销与修订查看。
- 依赖：WP09、WP10、WP15、WP16。验收：G11、G17、G30。

### WP19 完成架构拆分和可测试性收尾

- 将已受测试保护的 Runtime 责任迁到独立的账户会话、采集、规则、复盘、通知、生命周期服务；由组合根注入，明确每类状态唯一所有者。
- 拆分查询仓储、编辑仓储、行情缓存、迁移和备份文件服务；只保留必要共享连接工厂与事务辅助方法。
- 将 `ReviewServices.cs` 和大型 ViewModel 按用例/编辑器拆开，禁止服务直接操作 WPF 控件。采用可观察状态＋命令，消除散落的可变 delegate 和隐式当前账户依赖。
- 添加依赖方向检查、取消与释放测试；不以类名数量或文件行数作为唯一验收，也不引入没有使用场景的消息总线/微服务。
- 依赖：WP03–WP18。验收：G02、G20、G31。

### WP20 升级受支持运行时、固定依赖和 Bridge 产物

- 将 .NET 项目及基准/测试统一迁到 .NET 10 LTS，WPF 使用对应 Windows TFM。用 `global.json` 和依赖锁固定验收版本；后续安全补丁通过复验发布。[官方支持周期](https://dotnet.microsoft.com/en-us/platform/support/policy/dotnet-core) 显示 .NET 8 于 2026-11-10 结束支持，.NET 10 支持至 2028-11-14。
- 审查 SkiaSharp、SQLite/加密原生库及全部直接/传递依赖的兼容性和已知漏洞。不能只改 TFM 而保留不支持的新旧原生组合。
- 打包隔离 Python 3.13 x64 运行时与实测可用的 MetaTrader5/传递依赖，固定 wheel 哈希、解释器版本和来源；兼容性验证通过再升级 Python 主版本。当前 PyPI 有 CPython 3.13 Windows x64 构建，不能据此跳过当前 worker 的实际测试。[包发布信息](https://pypi.org/project/metatrader5/)
- 使用构建机预装配运行时，用户电脑不运行 pip、不依赖 PATH 和 `C:\Program Files\Python313`。Python 嵌入发行版不自带 pip，不能照搬现有 venv 安装脚本。[Python 嵌入说明](https://docs.python.org/3/using/windows.html#the-embeddable-package)
- Bridge 在隔离构建目录编译，确认本次编译日志无错误及产物来源/哈希；不能因为旧 `.ex5` 存在就报告成功。记录 MetaEditor build，配置发现路径，不硬编码 WeTrade。
- 工具链不能保证字节级相同的产物时，明确记录非确定性来源、签名前哈希和来源证明；不把 Deterministic=true 等同所有产物可复现。
- 依赖：WP19。验收：G01、G21、G31、G32。

### WP21 正式安装、修复和卸载

- 主分发采用签名的全信任桌面 MSIX＋App Installer；保留包含相同运行时的便携包作为支持与离线备用。拟建 `packaging/msix/`，固定包身份/发布者，生产身份不得随版本变化。
- 验证托盘、开机启动、跨进程命名管道、Python 子进程、MT5 目录访问及 Bridge 安装在包身份下可工作；显式检查依赖与权限，无需用户装开发环境。
- 包安装目录只读，用户数据由统一 StorageLocator 管理。验证 MSIX 路径虚拟化，迁移便携目录时不得因路径变化出现“看似数据丢失”的第二套空库。
- 默认普通用户安装，开始菜单/图标/版本可见；第二次启动激活现有实例。修复安装保留资料，卸载清理程序和自启动；是否保留工作区有清楚说明和导出入口。
- 干净电脑验收：没有 .NET/Python/源码仓库，中文/空格用户名、不同盘符、离线、App Installer 缺失/被策略禁用均有明确结果。
- 依赖：WP09、WP11、WP20。验收：G28、G32、G33。

### WP22 安全更新和兼容回退

- 发布地址使用 HTTPS，包身份与签名验证固定；稳定版与候选版分开，用户默认只获取稳定版。更新索引包含最小系统、数据/协议兼容性与说明。
- 自动检查更新并展示版本说明，可稍后安装；不在交易监控过程中强制终止进程。默认采用启动检查及关闭后应用，不启用无条件强制降级。
- 下载中断/错误签名/错误版本不得覆盖现有程序；更新前完成草稿、备份和工作区状态标记，新版启动迁移失败进入恢复界面。
- App Installer 负责程序包更新，WP07/WP08 负责数据恢复；二者不是同一事务。保留旧包用于支持，schema 不兼容时旧包不得直接写新库；优先发布兼容的前向修复。
- 演练从便携旧版迁入 1.0、候选版间升级、坏包、断网、签名变更和迁移后首次启动失败；恢复不丢升级后新增资料。
- 依赖：WP08、WP10、WP21。验收：G14、G33、G34。

### WP23 CI、供应链和发布门禁

- 建立 Windows 流水线：干净 checkout、锁定恢复、.NET/Python 测试、WPF 编译、依赖审计、格式/静态分析、只读能力检查、发布内容和测试证据归档。
- 对共享 `bin/obj` 的构建顺序执行或使用隔离输出目录，不能让并行测试互锁而产生假失败。原生桌面 UI 测试使用交互式 Windows 测试机，不把无桌面的云 runner 当成可视验收。
- Bridge 使用明确安装工具链的受控构建机；生产签名使用保护凭据/受控签名步骤，普通 PR 不可读取密钥。签名私钥不进入仓库或 ZIP。
- 生成 SBOM、第三方许可清单、签名前后哈希、源码提交与构建证明；扫描直接/传递依赖和最终包中的 Python/原生库。
- 未获评估处置的可利用高/严重漏洞阻止发布。测试失败、未签名、缺运行时/许可证/哈希、残留真实账户数据都使门禁失败。
- 依赖：WP01、WP20–WP22。验收：G01、G31、G35。

### WP24 性能、容量与长时间运行

- 固定参考机：至少 4 核 CPU、16GB RAM、SSD、Windows 11 x64；报告具体硬件、OS、构建和数据集哈希。门槛为待实测的验收目标，不是现有性能保证。
- 数据集包含 10 万笔完整交易、30 万成交、多账户、规则/笔记、50GB 附件；常规基准不每次复制附件，单独容量测试覆盖大文件和大量小文件。
- 冷启动到可交互 P95 ≤5 秒，10 万笔冷查询 P95 ≤3 秒、热筛选 ≤200ms、单笔详情 ≤300ms；每类至少 30 次有效测量，固定冷/热缓存定义。
- 4Hz 采集模拟下 P95 处理延迟 ≤250ms；复盘/导出同时运行相比空载新增 P95 ≤50ms；取消请求在 500ms 内反馈且后台最终停止。
- 连续 72 小时模拟采集/编辑/切账户/断线，再加两套终端各至少 24 小时只读观察。区分外部终端故障与应用故障，记录恢复时间和事件计数。
- 常规 10 万笔工作负载私有内存稳定后 ≤1GB，导出额外内存 ≤300MB；稳定阶段私有内存、线程/句柄无持续线性增长。测 p50/p95/p99、CPU、队列长度、DB/WAL 增长和附件解码峰值。
- 50GB 备份/恢复以流式工作，在参考机上各 ≤30 分钟；自动备份故障不冻结界面、不持续撑满磁盘。未达标先定位和修复，不在验收末尾无记录放宽门槛。
- 依赖：WP19–WP23。验收：G08、G18、G22、G36。

### WP25 原生 WPF、可访问性和兼容性验收

- 建立独立桌面自动化：首次引导→连接/离线→筛选→编辑→日记→评价→回放/批注→导入→导出→备份→重启验证。使用合成工作区，绝不点真实交易按钮制造订单。
- 覆盖 Windows 100%/150%/200% 缩放、跨屏不同 DPI、窗口最小尺寸、亮/暗/高对比度、中文/英文区域格式。
- 每个交互控件有稳定 AutomationId、可读名称和键盘顺序；焦点可见、对话框焦点返回正确，Narrator 可读保存/错误状态；不以颜色单独传递盈亏和风险。
- 高频流程鼠标和纯键盘均完成；桌宠穿透/隐藏/锁定后可通过托盘恢复；启动/退出/升级不丢窗口位置或留下幽灵托盘。
- 旧 V20 未验收部分纳入此处。原生截图/录制、环境和结果入档；工具拿不到原生窗口时记录未通过验收，不用 XAML 编译代替。
- 依赖：WP16–WP24。验收：G06、G25–G28、G32、G33、G36。

### WP26 独立复核和完整回归

- 审核 F01–F10 的修复，不仅检查新增测试是否通过，还核对调用链、共享状态、回执应用位置与故障注入覆盖。
- 复核原 R01–R12、V01–V20，尤其计划绑定、行为提醒去重、目标冻结、时区、数据库不可写、行情未来信息及附件删除后的恢复。
- 在完全隔离样本上实施并发压力、随机取消、异常帧、磁盘满、权限错误和进程结束；检查失败结果是否与实际数据状态一致。
- 对新发现的问题编号、定级、给定向测试并修复；直到本版主流程无 S0/S1/S2。未实现路径不可用“测试未覆盖”归为通过。
- 依赖：WP01–WP25。验收：G01–G34、G36、旧 V01–V20，以及 G35 的工程产物部分；G35 的授权与最终文档由 WP27 补齐，WP28 汇总确认全部门槛。

### WP27 用户文档、授权和支持交付

- 完成安装/首次使用、离线工作区、指标字典、备份恢复、换电脑、升级失败、日志诊断、数据删除、已知限制说明；文档对应实际 UI。
- 提供 `CHANGELOG.md`、产品许可文本、第三方 notices、隐私说明、安全问题联系渠道及依赖更新流程。产品署名和资产权属由实际权利人确认，不自动选择开源许可。
- 核对桌宠图片/音效/字体、Python/MetaTrader5、SQLCipher/OpenSSL、SkiaSharp 和构建工具的分发权利；不能把“可下载”当作允许随产品重新分发。
- 提供可工作的支持地址和故障分类模板；默认只收用户主动发送的脱敏诊断。安全更新与普通修复分别记录，不承诺无人负责的响应时限。
- 依赖：WP20、WP25、WP26。验收：G35、E01–E05。

### WP28 生成正式候选包并执行发布交接

- 冻结一个提交，执行全套测试/验收，生成源码版本标签、签名安装包、完整便携包、更新索引、SBOM、校验清单、发行说明及验收报告。
- 发布候选包做最后干净安装和更新/恢复冒烟；测试同一签名产物，不能测完后从其他未验证目录手工拼包。
- 任一外部条件未满足，报告精确缺项和可交付候选产物；不得使用自签名测试证书冒充正式签名，不代用户购买证书/许可证。
- 满足全部门槛后，交付可正式发布的包及清单。把文件公开上传、切换稳定更新源属于最终发布动作，执行时核对目标地址和用户已有授权；本次编写计划不进行上传。
- 依赖：WP26、WP27、E01–E05。验收：全部门槛及发布清单。

## 6. 数据与恢复设计的特别要求

### 6.1 版本与审计

- 区分 schema 版本、业务源版本、规则版本、文档修订、协议和文件格式版本，禁止用一个版本号替代所有兼容性判断。
- 新增记录建议包含 `source_namespace`、原始来源 ID、记录时间、事件时间、迁移/导入批次和操作关联 ID；具体列名由 WP02/WP08 定义，不在原始 MT5 表上直接改写人工字段。
- 数据库层验证账户与实体的引用完整性、非负修订和合法枚举；旧脏数据先报告/隔离，不能为了加约束删掉无法识别的人工文档。
- 重新计算是可重复的派生过程，保留原始事实和人工资料。初次迁移/恢复历史不触发桌宠旧提醒。

### 6.2 发布版的恢复保证

| 故障 | 必须表现 |
| --- | --- |
| 保存失败/磁盘满 | dirty 保留，显示失败，可导出草稿；不能显示已保存。 |
| 普通退出/升级退出 | 等待持久化；失败有保留与取消退出路径。 |
| 进程崩溃 | 重启发现上次已持久化草稿和恢复状态，显示恢复结果，不伪称未落盘输入已恢复。 |
| 数据库损坏 | 保留原集、副本抢救、备份恢复和缺失报告；不自动掩盖人工数据损失。 |
| 恢复中断 | 重启根据操作日志继续或回滚，先验证有效数据集再清理。 |
| 密钥错误/丢失 | 明确解锁或恢复密钥流程，绝不当作数据库损坏；无密钥时如实说明无法解密。 |
| 更新后 schema 不兼容 | 阻止旧程序写入；保护升级后资料，允许前向修复或副本恢复。 |
| 整机/硬盘丢失 | 只有已导出到另一介质、且持有恢复凭据的有效备份可恢复；本机备份不提供该保证。 |

## 7. 统一验收矩阵

每一项记录：需求/WP ID、测试数据哈希、提交和包哈希、环境、步骤、期望、实际结果、日志或截图、日期。自动测试结果只证明覆盖的场景。

| 门槛 | 必须通过的场景与结果 |
| --- | --- |
| G01 | 干净 checkout 可构建；依赖锁与标签可追溯；源码及最终包无真实账户数据和密钥。 |
| G02 | Core/Application 不引用 WPF/具体存储；测试可注入时钟、调度、仓储和文件失败。 |
| G03 | A/B 同 position ID，A 编辑中切 B；A 草稿只归 A，B 编辑器清洁，过期回执不写 B。 |
| G04 | A→B→A 乱序详情、账户无交易、净额模式、离线账户切换不显示旧身份内容。 |
| G05 | 写入暂停→继续输入→旧回执→下一次保存：全部最新文本落库；冲突/失败保持 dirty。 |
| G06 | 全部编辑器的切换、刷新、退出、重启不覆盖有效草稿；保存状态可被键盘和读屏识别。 |
| G07 | 并发成交更正/文档保存期间读快照，业务内容和版本一致；缓存失效正确。 |
| G08 | 10 万笔账户单笔详情精确查询，导出无逐笔全账户扫描；查询计划和延迟符合 WP24。 |
| G09 | 至少 251 笔、页大小 100：全部导出准确 251 笔，选中导出准确匹配集合，各格式总额一致。 |
| G10 | 真实格式行为 ID 含测试账户/服务器路径时，所有公开输出均无泄露；使用包内引用 ID。 |
| G11 | 范围外机会附件不导出；缺失/篡改附件、CSV 公式前缀、HTML、路径穿越和超限文件正确处理。 |
| G12 | 数据库/附件每次移动前后注入失败，旧数据可读且哈希一致；不能被 finally 清掉最后有效集。 |
| G13 | 恢复各状态硬结束进程，重启可续行/回滚；后台查询/回放/写入不会访问半切换工作区。 |
| G14 | schema 1–8 到正式版，重复启动和各迁移点中断可恢复；新 schema 被旧程序拒绝写入。 |
| G15 | 损坏页、坏 WAL、有效部分表、无效 JSON、磁盘满分开处理；人工资料抢救数量可核对。 |
| G16 | 加密库/WAL/附件与临时文件无敏感明文；错误密钥不重建；密钥轮换中断可恢复。 |
| G17 | 加密备份跨 Windows 用户/另一机器恢复，口令错误/篡改被拒绝；旧备份迁入正常。 |
| G18 | 自动备份/保留/限额/不可达目标可观察；独立恢复演练满足数据和 WP24 容量要求。 |
| G19 | 启动异常、WPF 错误、后台异常和日志不可写可诊断；诊断默认无正文/账号/截图。 |
| G20 | 退出有界等待，无未观察业务任务；重启能识别不正常退出，进程/句柄不残留。 |
| G21 | Worker/Bridge 协议不匹配、超大/无效帧、Python 缺失/被结束都有准确状态和清理。 |
| G22 | 断线、休眠、切账户、历史缺块与重试不丢失/重复事实、不补弹旧提醒；压力下有界。 |
| G23 | 金标准核算交易/现金/净值、费用/入出金、多个币种与未知风险；重复同步总账不变。 |
| G24 | DST/服务器改时/PC 改时/跨日部分平仓正确归属日期，历史不套用当前偏移。 |
| G25 | 回放没有未来 K 线/证据，取消和切交易不串画面；缺行情明确降级。 |
| G26 | 批注锚定时间/价格，缩放和重启后保持；事后批注不出现在其记录前的回放中。 |
| G27 | CSV 编码/时区/币种映射、坏行、更正、重复导入及批次撤销均有确定结果和审计。 |
| G28 | 无 MT5/无网络仍能浏览编辑已存资料；演示数据与真实工作区隔离；新用户可完成引导。 |
| G29 | 指标有统一字典与样本分母，区间计算正确，重叠/相关样本不产生无依据因果或显著性结论。 |
| G30 | 账户删除覆盖关联/索引/草稿/缓存；附件仍被其他实体引用时保留；清理有预览和审计。 |
| G31 | 全套 .NET/Python 测试、构建/静态检查/原生依赖兼容通过；Bridge 本次编译失败不能复用旧产物。 |
| G32 | 无开发环境的两台干净 Windows 机器安装成功；中文路径、普通用户、离线依赖、签名验证通过。 |
| G33 | 便携迁入/修复/卸载/重装正确；包身份不造成双库；升级与备份恢复可实际完成。 |
| G34 | 坏签名、断网、更新中断、首次启动迁移失败不毁坏原工作区，兼容回退不丢新增资料。 |
| G35 | SBOM、许可、支持渠道、隐私说明、发行说明和所有最终文件哈希齐全；发布凭据受控。 |
| G36 | WP24 性能/容量/72 小时测试及 WP25 多 DPI/键盘/读屏/原生流程均有通过证据。 |

## 8. 发布工具链决策与外部条件

### 8.1 已确定的技术方向

- .NET 10 LTS、自包含 Windows x64、隔离 Python、维护中的 SQLite 加密原生库；所有实际补丁版本以验收锁文件为准。
- MSIX 主安装包＋App Installer 更新检查＋完整便携备用包；真实包身份下的 MT5 集成、路径与普通用户权限必须先验证。
- Windows 原生测试机承担 WPF/MT5/安装升级验证；无原生窗口工具时不能宣称验证完成。
- 普通开发使用定向测试，候选版执行完整回归/容量/长跑；持续监控依赖生命周期，正式发布后安排安全补丁维护。

技术依据：MSIX 面向外部用户分发须使用目标机器信任的签名；测试自签名证书不能替代生产信任。[Microsoft 分发与签名说明](https://learn.microsoft.com/en-us/windows/msix/app-installer/installing-windows10-apps-web) 更新策略可配置启动检查、提示与强制行为，不能把系统包更新等同数据库回滚。[App Installer 更新配置](https://learn.microsoft.com/en-us/windows/msix/app-installer/update-settings)

数据库加密需要专门 SQLite 实现；Microsoft 文档的演示 bundle 并不是受支持产品承诺。[Microsoft SQLite 加密说明](https://learn.microsoft.com/en-us/dotnet/standard/data/sqlite/encryption) SQLitePCLRaw 维护者已说明旧免费加密 bundle 弃用，因此 WP09 要建立可维护构建/更新路径，不能直接套用旧教程。[维护者说明](https://github.com/ericsink/SQLitePCL.raw/wiki/SQLite-encryption-options-for-use-with-SQLitePCLRaw)

### 8.2 必须落实但不能虚构的条件

| 条件 | 具体内容 | 未具备时的状态 |
| --- | --- | --- |
| E01 发布者与资产授权 | 产品名称/发布者身份、产品许可选择、桌宠及第三方分发凭据/许可确认。 | 可开发候选版，不公开分发权利未明确的资产或二进制。 |
| E02 生产签名 | 用户控制的有效签名证书或可信签名服务、时间戳与密钥使用方式。 | 生成待签名产物和脚本；自签名仅标测试用途。 |
| E03 分发地址 | 用户控制的 HTTPS 下载/更新地址、发布账号与稳定频道，维护支持联系方式。 | 本地候选包可验收，公开更新链路未验收。 |
| E04 真实验收环境 | 两台干净受支持 Windows 机器，交互式桌面，两种券商终端的只读观察环境。 | 对应 G 项保持未完成，不用模拟结果替代全部实机证据。 |
| E05 仓库与构建身份 | 真实 Git 作者、版本仓库/CI 目标、合法可用 MetaEditor 构建机和依赖维护负责人。 | 本地清单与脚本可准备；缺失身份不伪造，受控发布构建待落实。 |

这些条件在实施早期登记；能独立完成的工程继续推进，不反复要求确认普通修复。只有确实需要购买、授权资料或公开发布动作时处理对应事项。

## 9. 最终交付清单

候选发布目录建议为 `artifacts/release/1.0.0/`，至少包含：

| 文件/证据 | 内容 |
| --- | --- |
| `TradePet-1.0.0-win-x64.msix` | 已签名主安装包，包含完整运行时和已验证 Bridge。 |
| `TradePet.appinstaller` | 正式身份及 HTTPS 更新地址；与发布频道一致。 |
| `TradePet-1.0.0-win-x64-portable.zip` | 同提交同运行时的便携备用包，明确数据路径和更新方式。 |
| `checksums.sha256`、`release-manifest.json` | 包哈希、源码提交、工具链、协议/schema/格式版本和支持平台。 |
| `sbom.spdx.json`、`THIRD-PARTY-NOTICES.txt` | 最终依赖及分发许可，包含 Python/wheel 和原生库。 |
| `RELEASE_NOTES.md`、用户手册 | 修复、功能、兼容范围、升级和恢复操作、支持渠道。 |
| `acceptance-report.md` | WP、F、G、旧 V 编号映射，以及实机/回归/长跑/升级证据路径与结果。 |
| `performance-report.json`、诊断样例 | 参考机、原始测量、分位数、脱敏示例。 |
| 签名验证与构建日志 | 本次最终产物验证，不能引用旧发布目录的历史日志。 |

最终报告明确区分“功能实现完成”“候选包验收通过”“生产签名完成”“正式发布完成”。四者不能互相代替。

## 10. 执行与同步规则

- 下一项是 WP01：建立可追溯基线。之后按依赖推进，优先闭合账户隔离、草稿保护、读快照、导出和恢复。
- 本计划的全部工作作为一个正式版本交付，不能在修完几个问题后把剩余门槛改成“以后优化”。
- 每次完成一个 TODO 项记录修改文件、为何修改、定向验证、仍存在风险和下一项；未完成项保持未勾选。
- 规格变动须更新本文及关联 G 项，说明理由和对发布承诺的影响；预算/工期压力不是降低数据正确性或隐私门槛的理由。
- 本计划不承诺“软件没有任何缺陷”；承诺支持范围、已知风险、可执行验收和故障恢复能力均可追溯。
