# TradePet 复盘功能统一修改方案

日期：2026-09-06

状态：R01–R12 已实现，自动化、十万笔性能、真实终端只读行情和 Release 发布链路已验收。V20 的多缩放档实窗截图因当前 Codex 会话未暴露原生 Windows 窗口控制接口而保留为人工视觉确认，未将该限制伪装成自动通过。本文仍是完整交付范围，不按批次、阶段或 MVP 拆分。

## 1. 修改目标与统一决策

把当前以统计卡片、分类表和逐笔归类为主的复盘页，改为可以完成以下过程的复盘工作区：

**查看交易事实 → 保存当时的理由和证据 → 评价执行过程 → 找出重复问题 → 设置个人改进规则 → 在后续交易中检查执行情况。**

整体沿用 Windows WPF、MT5 只读采集和本地 SQLite。现有胜率、盈亏因子、期望值、多维分组、计划匹配、行为规则、R 和波动统计继续复用；新增功能统一使用同一套账户身份、交易标识、筛选条件、数据来源和指标定义。

本方案作出以下明确选择：

- 新增逐笔档案、日历和日记、策略规则模板、执行评价、交易想法分组、行为证据分析、改进追踪、真实交易过程回放、机会观察记录、复盘导出与完整备份，均纳入同一交付范围。
- 原始成交、盘中观察、人工填写、历史行情补齐、规则重算分别保存来源；不能用事后解释覆盖原始事实。
- 桌宠继续负责盘中提醒；复盘负责解释和验证。用户启用个人改进规则后，复盘与桌宠共享该规则版本。
- 原“隔离回放”保留为诊断用例，名称改为“规则演示”；新的“交易回放”读取真实历史成交和可用行情，两者入口和存储隔离。
- 净额/交易所账户完整交易投影不在这份复盘功能扩展中启用；沿用当前仅监控边界。这是明确的范围限制，不将其标成已解决。
- 云同步、社交/导师协作、自动下单、通用回测撮合器、AI 自由生成交易评价不在本方案范围。自动日/周总结采用可追溯的结构化模板，所有结论能点开对应交易。

## 2. 成熟产品依据与取舍

以下产品事实来自官方功能说明，核对日期为 2026-09-06；没有登录付费账户做实机评测，也不采纳营销页面中的收益承诺。表中“TradePet 决策”为针对本项目的设计判断。

| 产品 | 官方已经明确的能力 | TradePet 决策 |
| --- | --- | --- |
| TradeZella | 单笔页面整合执行记录、图片附件、交易笔记与当日笔记。[交易详情说明](https://help.tradezella.com/en/articles/5860216-understanding-the-trade-page) | 建立一笔交易的统一档案；日记按交易日共享，不复制到每笔交易里。 |
| TradeZella | 策略规则可与交易关联，交易可标记已复盘，Progress Tracker 支持个人规则的每日检查。[使用流程](https://help.tradezella.com/en/articles/13863136-getting-started-with-tradezella) | 增加策略版本、执行清单、复盘状态与改进目标；盘前规则冻结，盘后评价留版本。 |
| Tradervue | 提供时间、品种、标签与风险等报告；允许调整 executions 的交易分组。[报告说明](https://help.tradervue.com/article/3423-reports-and-statistics)、[拆分与合并说明](https://help.tradervue.com/article/3480-split-merge-trades) | 补齐筛选、对照和明细下钻；另建“交易想法”分组，不直接改写 MT5 原始成交或现有 position 投影。 |
| Edgewonk | 将入场、管理、退出决策与表现关联，提供纪律观察、笔记和错过机会记录。[心理与纪律日志](https://edgewonk.com/trading-psychology) | 将执行质量与盈亏分开评价；情绪只由用户自报；未成交机会单独统计。 |
| TraderSync | 可以从交易详情进入回放，检查当时的执行和市场情境。[回放入口说明](https://tradersync.com/support/how-do-i-replay-an-existing-trade/) | 为真实交易提供行情与事件同步回放，不沿用固定示例充当历史回放。 |

产品之间的功能不做机械堆叠。TradePet 的重点是本地可追溯记录，以及“复盘发现的问题能进入桌宠提醒，并在后续复盘中检查”。

## 3. 当前代码与实际缺口

本节是当前源码事实，后文是待实现设计。

| 当前能力 | 依据 | 缺口与处理 |
| --- | --- | --- |
| 策略、形态、标签、计划内外人工归类 | [ReviewModels.cs](src/TradePet.Core/Domain/ReviewModels.cs)、[Rows.cs](src/TradePet.App/ViewModels/Rows.cs) | 没有交易笔记、附件、复盘状态和独立详情；新增档案，保留现有归类。 |
| 基础绩效、时段/品种/策略分组、R 和 MAE/MFE 汇总 | [ReviewAnalyticsCalculator.cs](src/TradePet.Core/Trading/ReviewAnalyticsCalculator.cs) | 缺少图表与交易联动、双组对照；统一回撤与覆盖率口径。 |
| ReviewFilter 支持策略、形态、单标签条件 | [ReviewModels.cs](src/TradePet.Core/Domain/ReviewModels.cs)、[MainWindow.xaml](src/TradePet.App/Views/MainWindow.xaml) | UI 主要只有日期、品种、方向；补齐筛选和多标签组合，不能把已有分组重复实现。 |
| 行为评估和单独时间线 | [BehaviorAnalyticsCalculator.cs](src/TradePet.Core/Trading/BehaviorAnalyticsCalculator.cs)、[TradePetRuntime.cs](src/TradePet.App/Runtime/TradePetRuntime.cs) | 周期评估有聚合结果，但没有统一的事件—交易证据关联，用户无法完整下钻。 |
| 保存 TradeExcursion 极值、覆盖信息和账户 EquitySample | [AppDatabase.Review.cs](src/TradePet.Infrastructure/Persistence/AppDatabase.Review.cs) | 没有完整的逐仓位盈亏时序；仅凭极值不能生成真实过程曲线。 |
| 结构化交易计划已有版本化行为 | [TradePlanMatcher.cs](src/TradePet.Core/Trading/TradePlanMatcher.cs) | 一次具体计划与长期策略模板仍需分离；新增模板版本和逐项执行证据。 |
| 固定合成场景 ReplayCoreScenarioCoreAsync | [TradePetRuntime.cs](src/TradePet.App/Runtime/TradePetRuntime.cs) | 是规则演示，不是用户历史行情回放。 |
| 数据库迁移版本 1–3，已有损坏恢复和备份目录 | [SchemaMigrations.cs](src/TradePet.Infrastructure/Persistence/SchemaMigrations.cs)、[TradePetPaths.cs](src/TradePet.Infrastructure/Persistence/TradePetPaths.cs) | 需增加复盘附件和人工数据的一致性备份；不重复建另一套账户数据库。 |

需同步纠正的统计表达：

1. 当前回撤百分比由“回撤额 ÷ 累计已实现利润峰值”得到；现有测试甚至正确复现约 153.85%。它不能被无标签地称为“账户资金回撤率”。见 [ReviewAnalyticsTests.cs](tests/TradePet.Core.Tests/ReviewAnalyticsTests.cs)。
2. 当前 MAE/MFE 卡的覆盖提示复用了具备初始风险的样本比例。波动采样覆盖和初始风险覆盖要拆开。
3. 当前逐笔时间通过本机时区格式化，而分组依赖服务器偏移；新增页面必须统一时间设置，并显示历史时间来源是否可靠。
4. 当前 Runtime 查询直接加载、组合多个集合；新复盘不能继续增加其长耗时职责，也不能在浏览复盘时向真实提醒链路补发历史事件。

## 4. 页面结构和共同交互

应用原有顶层导航保留。“复盘分析”内部统一为六个入口，交易详情使用右侧面板或独立详情视图，不再新增一排零散一级菜单。

| 入口 | 默认内容 | 主要操作 |
| --- | --- | --- |
| 总览 | 精简绩效卡、盈亏/回撤图、待复盘数量、数据覆盖提示 | 框选日期、点击异常点进入交易列表 |
| 日历与日记 | 月历、选中日现金账、当日交易过程、盘前/盘后笔记 | 写日记、查看事件、完成当日复盘 |
| 交易档案 | 原始交易、交易想法分组、待复盘/已复盘列表 | 搜索、批量归类、打开详情、管理分组 |
| 分析 | 分类表现、执行与结果、行为证据、对照分析 | 保存筛选、打开样本、查看规则版本 |
| 策略与改进 | 策略模板、执行清单、个人目标、周/月复盘 | 建立版本、启用提醒、检查目标 |
| 机会记录 | 观察到但没有成交的机会与主动放弃记录 | 记录理由、附图、关联策略 |

公共工具栏包含账户、日期、品种、方向、策略、形态、标签、复盘状态、执行评价、交易想法和搜索；高级条件折叠显示。默认单账户，不把不同账户货币直接相加。导出和数据状态作为公共操作。

共同交互约定：

- 标准统计默认按完整交易的平仓服务器日选样本；日历现金账按源成交/费用的发生日记账，页面明确标注口径。
- 从日历进入某天时，展示“该账户当日全量”；若附加品种等条件，标题改为“筛选结果”，不能让部分样本冒充整日账。
- 点击任一指标或行为必须进入产生该数字的确切样本，返回时恢复原筛选和滚动位置。
- 详情切换交易不能丢草稿；显示“保存中 / 已保存 / 保存失败 / 修改冲突”。只有数据库提交后显示已保存。
- 支持键盘上一笔/下一笔、保存、标记完成；图表有文本摘要和可访问数据表，盈亏不只依赖红绿。
- 空白、没有样本、没有权限读取本地文件、历史缺口、采集离线分别显示；未知值显示“缺少数据”，不填 0。
- 所有新增功能以 WPF 实际窗口的截图和真实交互验收，不以模型单元测试替代界面验收。

## 5. 完整功能规格

### R01 · 统一筛选、检索与复盘队列

- 补齐现有策略/形态/标签筛选；多标签提供“全部满足”和“任一满足”，标签去重、统一大小写匹配规则。
- 支持按品种、positionId、ticket、笔记正文搜索；保存命名筛选，保存后显示其账户与口径。
- 复盘状态为“待复盘、草稿、已复盘、需重审”；交易与日期分别维护，正在持仓只能保存草稿。
- 交易完成后进入待复盘；排序可选最大亏损、最大回吐、发生规则事件、最早未完成，默认按时间。
- 完成交易复盘至少要求一项执行评价和一条总结；可明确选择“按计划正常结束”。情绪、截图不设强制项。
- 标记完成记录该笔源数据和规则版本；后续补入费用、修正成交或更换评价依据时改为需重审，保留原结论。
- 批量操作支持策略、标签、复盘状态；批量完成前显示影响数量，不静默把未评价交易全部判为合规。

### R02 · 逐笔交易档案

统一详情包含五块内容：

| 内容 | 必须显示或允许填写的字段 |
| --- | --- |
| 成交事实 | 账户/货币、品种方向、首开/加仓/部分平仓/最终退出、各笔数量和价格、费用分解、完整交易净盈亏 |
| 原始计划 | 绑定的具体计划和策略版本、预期入场区、初始止损与目标、首开风险金额及来源 |
| 交易过程 | 持仓变化、止损止盈观察值、关联行为事件、真实提醒是否交付、采样缺口 |
| 人工复盘 | 入场理由、退出理由、做对的事、待改进的事、下次执行动作；可选的情绪/市场环境标签 |
| 证据 | 粘贴或选取本地 PNG/JPEG/WebP、图片标题、采集/补录时间、对应事件；缩略图和放大查看 |

记录约束：

- 盘前计划、盘中笔记、盘后复盘使用不同时间字段；用户可填写发生时间，但系统记录时间不可改写。
- 图片导入到受控附件目录，不只保存原始外部文件路径；不会自动截取其他窗口。行情回放图和当时截图有不同来源标签。
- 交易日志可显示当前观测到的止损变化；未运行期间没有采到的每次修改不得凭空重建。
- 自动建议标签可以接受或撤销；原始规则事件保留，人工异议单独记录。
- 原始 MT5 价格、费用和成交数量不在笔记编辑器中修改。来源修正须走采集重同步和版本更新。

### R03 · 日历、日记和每日过程总结

- 月历显示当天已实现交易现金盈亏、首次开仓数、待复盘数、笔记和数据缺口标记；支持月汇总与日期定位。
- 每日页面包含盘前计划/状态、日内记录、盘后总结三个区块；无交易日也可以记录“主动休息 / 无合适机会”。
- 当日过程按时间展示成交、持仓和规则事件；跨日仓位显示“前日持有”，部分平仓当天已兑现的盈亏计入当天现金账。
- 自动生成可展开依据的事实卡：达标前后交易、连续亏损后的开仓、冷静期内开仓、费用总额、复盘完成率。
- “达标后新开交易最终净盈亏”只统计首次开仓发生在首次达标之后的完整交易；未平仓单列。不得把达标前开、达标后平的整笔盈利混入。
- “达标后发生的成交现金盈亏”单独统计发生在该时点后的成交/费用，与上一条使用不同标题。
- 首次达标时点必须有当时生效的目标版本和可用日内记录。没有可靠时点时显示无法判断，不能从最终日状态倒推一个时间。
- 手工确定交易日结束后可以生成日总结；后续又有交易或费用补齐时提示“日数据更新，需要重审”。
- 日总结模板包含：今天的执行事实、一个做对的动作、一个待改进行为、关联交易、下一次检查事项。

### R04 · 策略模板与执行评价

增加长期可复用的策略模板（Playbook），与现有某日某次具体交易计划分别建模。

- 模板含名称、适用品种/环境说明、入场条件、风险条件、管理/退出条件、示例图片和失效条件。
- 模板修改建立新版本；交易绑定开仓前存在的版本。后补策略绑定显示“事后归类”，不能充当盘前合规证据。
- 每项规则的结果为“通过、未通过、未知、不适用”，并记录自动事实或人工判断来源。
- 情绪和“当时为何退出”只允许自报，不因亏损或连亏自动推断恐惧、愤怒等心理状态。
- 展示入场、管理、退出三类执行情况。汇总采用透明的规则遵守率，不生成不可解释的综合交易分。
- 执行结果与盈利/亏损/保本交叉展示；执行资料不全的交易进入“待评价”，不能自动归入合规。
- 关键规则任一未通过即标为“有违规”；所有适用规则已知且通过才能标为“合规”。模板没有可评价规则时为未知。
- 策略页展示不同版本的样本数、表现与执行率；用户可查看全部版本，但必须显示版本混合，不暗示这是同一套规则的效果。

### R05 · 交易想法分组

新增独立复盘实体，用于把一次观点中的多个 position 联系起来。

- 用户在交易列表勾选同账户、同一精确品种的多笔交易，命名交易想法；允许跨日和双向成员，双向明确标为“含方向切换”。
- 一个原始 position 在同一账户最多属于一个有效交易想法；分组变更有记录，可解除或重新分组。
- 分组显示观点说明、整体时间范围、成员顺序、首次进入和重进次数、净盈亏、费用与执行评价。
- 基础交易计数继续按原 position；切换到交易想法视图时才按组统计，未分组交易作为单独单位。一个页面不得混合两种计数。
- 组净盈亏由成员按身份去重后求和；组最大同时敞口从成员成交时间重建，不能将各笔最大手数简单相加。
- 不平均入场价来隐藏不同方向，不把汇总 R 简单加总。数据不足时只显示金额和成员级 R。
- 自动化仅提供可解释的“可能同属一次想法”候选，用户确认后才成组；净额反手投影不能靠手动分组绕过。

### R06 · 图表、风险、费用和对照分析

统一提供以下可下钻图表：

- 按完整交易最终平仓排序的累计净盈亏及回撤区间。
- 每日已实现现金盈亏柱图与日历；账户净值观测曲线另设图层。
- 品种/方向/策略/形态/标签/星期/开仓小时/持仓时长分布，保留现有分组能力。
- MAE/MFE 与实际 R 的逐笔散点、盈利回吐与持仓过程图；缺采样的交易仍在列表中但不伪造点位。
- 两组同口径比较，例如同一策略的合规与违规、同一品种的两个时段；展示两组筛选定义和重叠样本数量。

分析约束：

- 分组可直接查看样本数、净盈亏、胜率、平均盈亏、期望值、平均 R、风险覆盖率和费用；各指标独立定义有效样本。
- 单组少于 30 笔时显示小样本提示；30 是产品提示阈值，不代表达到统计显著性。任何样本量下均不把相关差异写成原因。
- 金额比较同时展示首开量/已知初始风险的分布，避免仓位大小差异被误读为策略效果。
- 费用按原始 commission、swap、fee 分开展示，返佣保留正负号；账户级未分配费用单列，不强行摊到某笔交易。
- 入场参考价与成交价的差只叫“计划价偏差”。没有订单请求价与可核验的执行报价时，不展示“真实滑点”指标。
- 时间分析支持保存用户定义的交易时段。使用具名时区时按日期应用夏令时规则；无法确认券商历史偏移的记录标注估算来源。

### R07 · 行为事件、证据与相关损益

复用现有连续重进、亏损区持续、过度交易、亏损后放量、冷静期违规、计划偏离、利润回吐等规则，增加证据输出。

每个行为事件保存：

- 规则 ID、版本和阈值；实际事件时间、首次观察时间、所属账户与服务器日。
- 触发交易、提供上下文的前序交易，以及事件窗口。
- 使用的事实值、缺失数据、是否为实时观察/历史重算。
- 提醒是否交付、是否因恢复/优先级/去重被抑制，以及人工复盘解释。

报告展示“命中交易相关净盈亏”和未命中对照，不使用“本可避免损失”作为已证实结果。

- 同一交易命中多个规则时，每个规则组可出现一次；总览并集按交易去重，各组金额不得直接相加。
- 账户级回吐事件不直接绑定一笔作为“肇因”；关联当时持仓及随后交易，并明确关联角色。
- 行为按事件发生日分析，交易绩效按平仓日分析。筛选发生冲突时显示各自时间口径，不偷偷裁掉前序证据。
- 持久化的实时事件与用当前规则重算的历史结果分别展示。历史重算只写复盘分析记录，不触发桌宠提醒。
- 点击行为卡进入确切交易和事件；用户可以标记“判断依据不足”并填写解释，原始观察不可删除。

### R08 · 周/月总结、个人改进和桌宠联动

- 统一支持周、月或自定义周期总结，包含执行情况、重复行为、相关样本、数据覆盖以及自选下一项动作。
- 个人改进目标包含规则、适用账户/品种、开始日期、目标值、观察周期、基线快照和结束条件。
- 可自动检查的目标优先绑定现有规则，例如冷静期内开仓率、计划外交易率；主观目标采用人工检查，不编造自动结果。
- 一条目标启用后冻结测量规则版本；更改阈值建立新版本和比较起点，保留旧观察结果。
- 用户可明确启用对应桌宠提醒。启用前展示触发条件和频率；归档目标停止未来提醒，历史事实保留。
- 同一事件同时匹配原有风控和个人目标，只形成一次可见提醒，并保留两个关联来源。
- 周/月总结展示执行率及对应分母、违规次数、同口径前后表现。无合格样本时显示无法比较。
- 未发生触发机会的日子显示“不适用”；不能算作 100% 遵守，也不能简单算失败。
- 模板生成的结论只描述统计事实，每条引用交易或事件；用户编辑的总结保存成单独版本。

### R09 · 真实交易过程与行情回放

回放以某笔交易、交易想法或某个交易日为入口，支持播放/暂停、前后事件跳转、逐根 K 线、倍速和时间轴定位。

显示内容：

- 可获取的 M1/M5/M15/H1 K 线，成交点、当时可知的计划/止损/目标、关联提醒和笔记。
- 若存在对应历史 ticks，可选 tick 回放并显示实际报价间隔；不承诺所有品种都有逐 tick 历史。
- 已记录的逐笔盈亏采样使用独立曲线，缺口明确断开；当前仅有极值的旧交易不补出虚构曲线。
- 历史 K 线补齐只能称为“行情重建”，不能称为当时的屏幕录像、完整指标状态或真实滑点回放。

数据和行为约束：

- MT5 的历史 bars 受终端可用图表历史限制；Python bars/ticks API 接受 UTC 时间，失败可返回 None。方案据此要求范围、精度与失败状态显式返回。[bars API](https://www.mql5.com/en/docs/python_metatrader5/mt5copyratesrange_py)、[ticks API](https://www.mql5.com/en/docs/python_metatrader5/mt5copyticksrange_py)
- 只有 OHLC 时，不能假定同一根 K 线内部高低点出现顺序。逐根播放只揭示已经结束的 K 线；tick 模式仅用已到达的报价构建当前 K 线。
- 回放时隐藏游标之后的成交、最终盈亏、事后笔记和事后才写入的计划；开启“查看完整复盘”后才展示全部结果。
- 盘后补录的内容即便填写更早的发生时间，也不能成为回放游标处已经知道的证据。
- 行情缺失仍可做成交事件回放；显示“仅成交事件，缺行情”，不把周末无成交和查询失败混为一谈。
- 取数使用明确终端路径、账户校验和独立请求 ID；不更改终端登录账户，不在实时采集主循环内执行长历史请求。
- 回放游标、筛选、暂停和退出只影响回放状态；不改实时会话、日账、提醒去重或真实仓位。
- 本范围不提供假设下单撮合；机会观察和真实交易绩效始终分别统计。

### R10 · 未成交机会与主动放弃记录

参考成熟日志中的 missed trades，同时记录“合理放弃”，避免把所有没参与的行情都评价成错失。

- 字段含观察时点、记录时点、品种/方向、策略版本、预设条件、参考入场/止损/目标、截图、未参与理由和后续复盘。
- 理由可选“不符合规则、风险额度不足、主动休息、犹豫、没有注意到”等，允许自定义。
- 区分事前观察和事后发现；事后补录不能纳入“当时识别率”。
- 没有真实成交，默认不计算真实盈亏、R、胜率和纪律表现；不能把潜在最大盈利加到真实业绩里。
- 可与后续实际交易关联，但关联不会把观察记录自动变为另一笔交易。
- 统计以记录数量、理由分布、是否符合当时规则为主，不输出“你本来应该赚到多少”。

### R11 · 导出、草稿与完整备份

- 导出当前筛选的交易/成交/行为明细为 UTF-8 CSV；日记和周期总结为 Markdown/HTML；附件可随 ZIP 包导出。
- 导出包含口径、账户显示名/货币、时区、范围、数据版本、生成时间和覆盖率。positionId/ticket 在无损 JSON 中使用字符串，避免大整数精度丢失。
- CSV 提供安全的表格查看模式，处理公式前缀和超长标识；无损备份保留原始值，不以 CSV 作为唯一恢复来源。
- 复盘笔记输入后短延时保存草稿，显式保存立即提交；并发编辑使用版本检查，冲突时保留两个版本。
- 完整备份包含 SQLite 一致性快照、笔记修订、附件、策略版本、目标及清单哈希。沿用已有备份目录和恢复机制。
- 恢复先在临时目录检查包格式、路径、哈希、数据库完整性和账户身份；停止写入后再切换，切换前保留现有完整备份。失败时保持当前数据可用。
- 新查询可取消、导出可取消；失败或取消后的半成品不能显示为导出成功。
- 对外分享包默认隐藏账号标识和电脑路径，并提示图片可能含用户自行保存的信息；不会自动上传。

### R12 · 数据质量与旧数据兼容

- 页面统一展示：成交同步范围、持仓采样覆盖、初始风险覆盖、行情覆盖、盘前计划覆盖和人工复盘完成率。
- 每项指标可展开分子、分母、缺失原因及相关样本；空数据不显示“全量覆盖”。
- 旧交易已有策略/形态/标签/人工归类原样保留；新增复盘状态默认待复盘，规则执行默认未知，不倒填为合规。
- 旧记录若只有 MAE/MFE 极值，继续显示明确标记的旧指标；新口径的过程图不由这些极值反推。
- 历史服务器偏移不确定时保留既有归日，显示推断来源；用户修正时间设置会生成新的分析版本，不改原始 UTC。
- 数据库不可写时，历史查询可继续读取已提交快照；编辑保留未保存草稿并提示。恢复后按版本提交，不能先显示保存成功。
- 所有外部数据失败沿用“失败与空结果分开”的已修复契约，重连/历史补齐保持静默。

## 6. 指标口径与例子

### 6.1 统一定义

| 指标 | 口径 |
| --- | --- |
| 完整交易净盈亏 | 同账户、同 position 的源成交 profit + commission + swap + fee 之和；完成交易按最终平仓日进入绩效样本。 |
| 日历已实现现金盈亏 | 服务器日内发生的交易成交及明确属于交易成本的现金项之和，包含部分平仓，排除本金出入金；无法分配的费用单列。 |
| 胜率 | 盈利笔数 ÷（盈利笔数 + 亏损笔数）；保本另列。空分母显示无样本。 |
| 累计已实现回撤额 | 完整交易净盈亏累计曲线的历史峰值减当前值；保留货币金额。 |
| 利润峰值回吐比例 | 回撤额 ÷ 正的累计利润峰值，仅在详情显示此全名；不得冒充账户资金回撤率。 |
| 账户净值回撤率 | 在外部资金流可核验的连续区间内，按资金流前后的净值做单位化曲线，再以曲线峰值为分母；缺少流前/流后样本则分段并标缺口，不用累计利润作分母。 |
| 首开实际 R | 完整交易净盈亏 ÷ 开仓当时可核验的初始风险金额；加仓交易标记“以首开风险为分母”，不冒充全生命周期风险。事后估算独立显示。 |
| MAE/MFE | 新采样口径使用该 position 自首开以来已实现现金净额 + 剩余仓位浮盈亏，费用/隔夜费按来源核对避免重复；从该累计过程的负向/正向极值取值。 |
| 遵守率 | 通过项数 ÷（通过项数 + 未通过项数）；旁列规则评价覆盖率。未知和不适用不进该分母，不能据此掩盖评价缺失。 |
| 复盘完成率 | 已复盘完整交易数 ÷ 当前范围需复盘的完整交易数；草稿与需重审均未完成。 |
| 行为相关损益 | 具有指定关联角色的命中交易净盈亏，按账户 + position 去重；不解释为因果损失。 |

账户净值取样不完整时只展示观测曲线和观测最大回撤，注明它可能遗漏采样间波动。资金流时点无法校正的区间不输出完整回撤率。不同账户货币不做隐含汇率换算。

### 6.2 必须分开的覆盖信息

- 初始风险覆盖率：有开仓时风险证据的完整交易数 / 完整交易数，独立于盈亏曲线是否连续。
- 波动覆盖率：满足采样规则的交易数 / 完整交易数；交易内另列覆盖时长、最长缺口、采样间隔。
- R 统计不应因与 R 无关的 MAE/MFE 断采样而自动失效；初始风险证据缺失则 R 为空。
- 新旧波动算法保存版本。历史旧口径“剩余仓位浮盈亏极值”与新口径不得混合平均。
- 原有至少 90% 时长覆盖可继续作为波动统计的最低门槛，但同时要求首开后 2 秒内有样本、完成成交可核验、无超过 5 秒的持仓采样缺口；仍标为采样极值，不称连续真实极值。上述阈值保存为算法版本参数，不是连续行情完整性的证明。

### 6.3 验收计算例子

以下数字均为合成验收数据：

- D1 部分平仓净获利 40，D2 最终平仓净获利 60：日历 D1=40、D2=60；完整交易绩效 D2=100，两种日视图不能混算。
- 一笔交易同时命中“冷静期”和“放大仓位”，净亏 100：两张规则卡分别显示关联 -100；合并总览仍为 -100，不得变成 -200。
- 四笔净盈亏依次 +10、-5、+8、-20：累计峰值 13、回撤额 20；153.85% 只能叫利润峰值回吐比例，账户净值回撤率需要实际本金和净值资料。
- 初始风险 100，最终净利 150：首开实际 R=1.5；该交易中间断采样可以导致 MAE/MFE 不可靠，但不能单凭这一点删掉已有可靠分母的 R。
- 账户存入 1,000 且没有交易：现金交易盈亏为 0；不得生成策略盈利 1,000 或错误的回撤恢复。
- 没有交易机会且没有违规的一天：目标状态不适用；不得按零违规推导出“100% 执行优秀”。

## 7. 数据模型、存储与修订契约

### 7.1 身份、时间和来源

继续使用现有账户键与账户 + position 的身份语义，新增 `TradeKey(AccountKey, PositionId)` 类型封装。交易想法、模板、日记等新增实体使用独立稳定 ID；任何查询和关联都携带账户，不只传 positionId。

所有可编辑对象保存 revision、createdAtUtc、updatedAtUtc。证据另外保存 eventAtUtc、recordedAtUtc、source、sourceVersion、timeBasis；盘前快照、原始事件不可被更新接口覆盖。

来源枚举建议为：MT5 成交、盘中采样、当时计划、人工当时记录、人工补录、历史行情补齐、规则重算。回放可见性按来源判断：MT5 成交和历史报价可按可信原始发生时间重建，不因导入较晚而全部隐藏；计划、判断、笔记和规则重算必须同时检查发生时间与实际记录时间，不冒充当时已经知道的信息。

### 7.2 新增或扩展实体

以下是逻辑与存储目标，不要求把可重建图表结果全部存成表。

| 对象/表 | 关键内容与约束 |
| --- | --- |
| trade_review_documents | 账户 + position 唯一；复盘状态、笔记结构、revision、reviewedSourceVersion、reviewedRuleVersion |
| daily_journals / period_reviews | 账户 + 日期或周期身份；盘前/盘中/盘后内容、总结、完成状态与依据版本 |
| review_revisions | 文档身份 + revision；保存旧内容、变更来源和时间，支持冲突恢复 |
| attachment_assets / attachment_links | 哈希、格式、大小、受控相对路径、来源时间；链接到交易/日记/策略/机会，不能跨账户串用 |
| playbooks / playbook_versions | 模板与不可变版本，规则内容、适用范围、生效时间；已有具体计划关联版本 |
| trade_rule_assessments | 交易 + 模板版本 + 规则；四态评价、证据引用、人工说明和 revision |
| trade_campaigns / trade_campaign_members | 交易想法及成员；账户一致，成员在有效分组中唯一 |
| trade_observations / position_pnl_samples | 首开、止损观察、采样覆盖和逐 position 过程值；保存当时可知信息及算法版本 |
| behavior_occurrences / behavior_trade_links | 稳定事件键、规则版本、观察/重算来源；交易链接注明触发、前序证据、当时敞口等角色 |
| improvement_goals / goal_observations | 目标版本、启用状态、冻结基线、统计窗口、触发机会数及检查结果；关联既有提醒事件 |
| opportunity_records | 未成交机会独立身份、策略、观察与记录时间、原因、证据和关联真实交易 |
| market_data_ranges / market_bars / market_ticks | 终端来源、券商服务器、精确品种、UTC 范围、精度、覆盖状态、失败原因和数据版本 |
| server_time_segments | 账户、来源终端、已知或估计的服务器偏移区间；历史推断标记与版本 |
| review_saved_filters | 账户作用域、完整筛选 JSON、排序、版本；不保存实时会话状态 |
| review_data_versions | 成交/费用、元数据、采样、规则、时间解释版本；用于快照一致性、缓存失效和需重审判定 |

行情缓存不能只按 XAUUSD 等简称跨券商复用；有完整服务器身份也仍保留来源终端以便审计。tick 去重不得只依赖毫秒时间戳，应保存同毫秒序号或来源记录指纹。

### 7.3 迁移与保存

- 在现有 migration 1–3 后追加版本，实施时以最新数据库版本为准；不修改已执行的旧 migration。
- 保留 deals/trades 主键和人工 metadata；新增表采用复合外键或事务内账户校验，并建立账户 + 日期/position 索引。
- 将旧自由文本策略和标签迁入可索引映射时保留原文本；不把已有自由文本策略冒充开仓前存在的模板。
- 原始成交补齐、派生数据版本、复盘需重审标记在明确事务边界内推进。编辑接口使用 expectedRevision，过期写入返回冲突。
- 附件先写临时文件、校验并原子移入，再提交引用；未引用文件可回收，已引用附件不得在备份期间删除。
- 备份期间冻结人工写入并固定数据库快照版本，保证清单与引用的附件一致；恢复失败不覆盖运行中的健康数据。
- 缓存可删除重建，人工笔记、规则版本、截图和目标不能当缓存清理。

## 8. 应用架构与 API 契约

### 8.1 明确项目边界

现有 Core 继续承载纯规则。新增不依赖 WPF 的 `TradePet.Application` 项目，专门承载复盘用例和边界接口，使实际编排可在测试中运行。

依赖方向：

```text
TradePet.App → TradePet.Application → TradePet.Core
TradePet.App → TradePet.Infrastructure → TradePet.Application / TradePet.Core
```

Application 不引用 Infrastructure；Infrastructure 实现仓储/历史源等接口，WPF 启动处负责组装。无需给每个纯计算类再套接口。

| 职责 | 管理内容 |
| --- | --- |
| ReviewQueryService | 账户/筛选/版本化快照、列表分页、图表和样本下钻 |
| JournalService | 交易/日记/周期总结、草稿、修订、附件引用和已复盘状态 |
| PlaybookService / CampaignService | 策略版本、评价清单、交易想法和唯一成员约束 |
| BehaviorReviewService / ImprovementService | 事件证据、去重样本、目标检查、真实提醒关联 |
| TradeReplayService | 行情请求、精度/缺口、可控游标与历史可见性 |
| ReviewExportService | 按快照版本导出、附件清单及完整备份恢复协调 |
| DataQualityCalculator 等 Core 计算器 | 指标定义、覆盖率、四态执行评价、事件分组和对照计算 |

### 8.2 用例接口约定

- `QueryReview(request, cancellationToken)` 返回不可变快照；request 含账户、范围、统计单位、筛选、会话代次和查询 ID。
- `LoadTradeDetail(TradeKey, revisionToken)` 返回完整档案与证据，不使用 UI 当前选中账户作为隐式参数。
- `SaveJournal(command, expectedRevision)` 返回成功新版本、校验错误、修改冲突或存储失败；调用方不能把失败当成功。
- `MarkReviewed(TradeKey, sourceVersion, ruleVersion)` 检查完成条件，并冻结当时依据。
- `BuildBehaviorReport(request, ruleVersion)` 返回事件集合、成员身份和缺口；禁止调用真实通知出口。
- `EnableGoal(goalVersion)` 提交明确规则和频率，经过既有通知优先级/去重门控；历史回放没有调用权限。
- `LoadReplay(request)` 返回 requestId、来源、请求/实际覆盖范围、精度和失败信息；终端账户变更后旧响应作废。
- `ExportReview(snapshotToken, destination)` 固定快照，避免一半数据来自旧筛选、一半来自新筛选。

### 8.3 调度与状态

- UI 仅收集参数、渲染快照；不直接枚举 Runtime 正在修改的交易字典。
- 复盘查询使用数据库读快照和独立取消令牌，在后台执行；相同版本的图表可缓存。
- 切账户、切筛选或离开回放使旧查询失效。返回时校验账户、sessionGeneration、queryId，过期结果不得回写新页面。
- 实时状态门仅用于短快照复制和提交；历史读取、附件、导出和行情请求不占用实时状态门。
- 新任务纳入监督和退出取消；UI 保存等待数据库提交，退出先处理草稿并限制后台清理时长。
- 提醒的预检、提交、展示与延迟隐藏使用统一状态所有权；复盘启用目标不能再次引入后台枚举和泡泡优先级竞争。
- 纯历史查询不 upsert 真实提醒、交易或运行中的日账。需要持久化的分析缓存使用独立身份和算法版本。

### 8.4 历史行情协议

新增 `python/tradepet_mt5_history_worker.py` 及 C# 历史客户端，独立进程只做按需 bars/ticks 查询；现有实时 worker 不承担长行情请求。

- 使用明确 terminalPath 初始化，只校验当前账户，不调用账户切换。
- 外层沿用现有协议信封，扩展在专用历史进程内声明的 capabilities 和版本化 payload；不会让旧实时/Bridge 消费者误处理行情数据。
- 请求包括 requestId、expectedAccountKey、terminalId、symbol、timeframe、UTC 范围、bars/ticks 类型、最大返回量。
- 响应包括分块序号、来源身份、actualRange、coverageStatus、precision、payloadVersion 和 error；只有全部分块保存成功后标记完成。
- 规定 maximumBars=5000、maximumTicks=10000 每块；数据量超限继续分块，间隔边界去重，整体并发为 1。
- 取消超时可结束专用历史进程；不能结束用户的 MT5 终端或实时 worker。
- MT5 本身仍可能受长查询影响，必须按第 10 节验证实时延迟；必要时缩小查询窗口并暂停未完成请求，不宣称独立进程能消除全部争用。
- 本方案不要求新增 Bridge 截屏能力，用户图表图片通过本地导入；既有 Bridge 仍提供已支持的图表对象和时间事实。

## 9. 文件影响图与实现依赖

以下为实施时的文件规划。标为“新增”的路径当前尚不存在；这是实现入口，不代表已修改。

| 文件或目录 | 角色 | 计划改动 |
| --- | --- | --- |
| [ReviewModels.cs](src/TradePet.Core/Domain/ReviewModels.cs) / 新增 Domain/Review/ | 共享模型 | 保留现有模型兼容，新增文档、状态、模板、分组、来源和质量契约 |
| [ReviewAnalyticsCalculator.cs](src/TradePet.Core/Trading/ReviewAnalyticsCalculator.cs)、[ReviewQueryEngine.cs](src/TradePet.Core/Trading/ReviewQueryEngine.cs) | 绩效与查询 | 统一指标分母、有效样本、统计单位与图表数据 |
| [BehaviorAnalyticsCalculator.cs](src/TradePet.Core/Trading/BehaviorAnalyticsCalculator.cs) / 新增 Core/Review/ | 行为及复盘计算 | 输出证据和规则版本；增加质量、日账视图、执行评价与对照 |
| 新增 src/TradePet.Application/Review/ 及项目文件 | 可测试用例 | 承接复盘服务、仓储接口、通知意图和取消/版本契约 |
| [AppDatabase.Review.cs](src/TradePet.Infrastructure/Persistence/AppDatabase.Review.cs)、[SchemaMigrations.cs](src/TradePet.Infrastructure/Persistence/SchemaMigrations.cs) / 新增 Persistence/Review/ | 存储 | 追加迁移、仓储、修订、证据关联、查询索引与一致快照 |
| [TradePetPaths.cs](src/TradePet.Infrastructure/Persistence/TradePetPaths.cs) / 新增附件与导出适配器 | 文件生命周期 | 受控附件、备份清单、恢复校验和临时文件 |
| [ProtocolEnvelope.cs](src/TradePet.Core/Protocol/ProtocolEnvelope.cs) / 新增历史 payload | 历史接口契约 | 专用进程能力与请求/响应，保留已有实时协议兼容 |
| 新增 python/tradepet_mt5_history_worker.py、Infrastructure/Mt5/Mt5HistoryClient.cs | 历史行情 | 身份校验、只读取数、分块与取消；不阻塞现有实时循环 |
| [TradePetRuntime.cs](src/TradePet.App/Runtime/TradePetRuntime.cs) | 现有集成入口 | 迁出复盘查询/保存编排，输出短快照与证据，接入统一通知意图 |
| [MainViewModel.cs](src/TradePet.App/ViewModels/MainViewModel.cs)、[Rows.cs](src/TradePet.App/ViewModels/Rows.cs) / 新增 ViewModels/Review/ | 页面状态 | 独立复盘 VM、异步保存、错误/冲突、筛选与详情导航 |
| [MainWindow.xaml](src/TradePet.App/Views/MainWindow.xaml) / 新增 Views/Review/ | WPF 视图 | 六入口、详情、图表、日记、回放和导出交互；复用现有 SkiaSharp 绘制能力 |
| [TradePet.sln](TradePet.sln) 与受影响 csproj | 项目依赖 | 增加 Application 与无 WPF 集成测试项目，调整引用方向 |
| [Core 测试](tests/TradePet.Core.Tests)、[基础设施测试](tests/TradePet.Infrastructure.Tests) / 新增 tests/TradePet.Application.Tests/ | 回归与用例测试 | 指标、账户隔离、修订、恢复、Fake MT5 与真实用例编排 |
| [README.md](README.md)、[发布脚本](scripts/build-release.ps1) | 交付说明 | 更新功能/协议边界，打包新增历史脚本和应用程序集 |

实施依赖统一为：共享模型与仓储契约 → Core 计算 → Application 用例 → SQLite/行情/附件适配 → WPF 集成 → 用例与界面验收。

这是依赖顺序，不是拆分交付。实现时仍遵守仓库“一次只做一个活动 TODO”的规则；只有下述统一验收完成后，才能把这份方案标记为已实现。

## 10. 统一验收标准

下表继续作为稳定验收契约。2026-09-09 已完成 V01–V19 的自动化或真实只读终端证据；V20 已完成六入口、详情、编辑、回放、导出、快捷键、滚动容器和 PerMonitorV2 配置核验，多缩放档原生截图受本次会话界面能力限制，仍需在 Windows 桌面人工确认。

### 10.1 需求验收矩阵

| 验收 ID | 对应需求 | 必须通过的场景与结果 |
| --- | --- | --- |
| V01 | R01 | 策略/多标签 AND/OR/正文联合筛选；保存后重启恢复；指标下钻与列表样本一致。 |
| V02 | R01/R02 | 草稿保存失败不能标已保存；并发修订返回冲突；切换交易和重启后已提交笔记/附件仍在。 |
| V03 | R01/R12 | 已复盘交易补入费用后变为需重审，旧结论保留；重算不覆盖手工归类。 |
| V04 | R02/R09 | 成交、止损观察、当时计划、事后笔记来源可区分；回放不会提前看到未来证据。 |
| V05 | R03 | D1/D2 部分平仓按发生日进入日历；完整交易只按最终平仓日统计一次。 |
| V06 | R03 | 达标后新开交易与达标后发生现金流分别计算；未知达标时点不产出断言。 |
| V07 | R04 | 未知/不适用不算规则通过；合规亏损与违规盈利正确分类；新策略版本不改变历史评价。 |
| V08 | R05 | 双账户相同 position 无交叉；重复成员被阻止；分组/解除后账户总盈亏不变且计数口径明确。 |
| V09 | R06 | 利润回吐比例和账户净值回撤率分别显示；入金不当盈利；资金流样本缺失时净值回撤区间降级。 |
| V10 | R06/R12 | 可靠初始风险但采样有缺口时，R 保留、波动统计降级；新旧极值口径不混算。 |
| V11 | R06 | 分组和 A/B 对照显示各自样本与重叠；样本少有提示，零分母和全部盈利不显示伪造数值。 |
| V12 | R07 | 一笔命中两规则，并集损益只计一次；点击事件可看前序证据；历史重算不产生真实提醒。 |
| V13 | R08 | 冻结基线和目标版本；无触发机会不算成功；同一事件匹配两来源只弹一次；归档后不再弹目标提醒。 |
| V14 | R09 | 在可取得历史的测试终端播放一笔真实历史交易，bars/ticks 与成交可同步定位；纯事件和缺口模式可用。 |
| V15 | R09/R12 | Fake MT5 返回 None、空数组、截短区间、同毫秒多 tick、取消、断线切账户时，覆盖标记和身份处理正确。 |
| V16 | R10 | 事后发现和事前观察分开；机会记录不进入真实盈亏、R、胜率和规则执行分母。 |
| V17 | R11 | CSV 特殊字符/长 ID、HTML 转义、图片附件可导出；安全 ZIP 路径校验；取消不生成成功状态。 |
| V18 | R11/R12 | 旧库迁移可重复启动且原数据不丢；完整备份在临时目录恢复后笔记、图、规则、关联和哈希一致。 |
| V19 | R12/API | 查询期间账户 A→B，A 的迟到结果/保存回执不能覆盖 B；数据库不可写与恢复状态清楚。 |
| V20 | R01–R12 | 六入口、详情、编辑、回放、导出组成完整 WPF 流程；Windows 100%/150%/200% 缩放无关键遮挡，键盘流程可用。 |

### 10.2 性能与实时链路

验收环境为 Windows 10.0.26200、.NET 8.0.30、32 个逻辑处理器、约 34.18 GB 可用内存。基准生成 100,000 笔完整交易、300,000 条成交、365 个交易日，SQLite 文件约 95.4 MB。完整原始结果见 [review-benchmark-2026-09-09.json](artifacts/review-benchmark-2026-09-09.json)。

- 合成数据写入耗时 2790.98 ms；每页 100 条，工作区不会一次实例化全部行 VM。
- 冷查询及对照耗时 2904.03 ms；缓存就绪后 5 次查询 P95 为 0.2945 ms，分别满足复杂对照 ≤ 3 秒和缓存查询 ≤ 1 秒目标。
- 取消在 85.67 ms 内被观察到。4 Hz 基线脉冲 P95 为 14.20 ms，并行未缓存复杂查询时为 14.72 ms，额外 P95 为 0.52 ms，满足 ≤ 200 ms 目标。
- 取消、账户切换、过期查询能快速停止回写；后台清理有上限，关闭过程不得等待 WPF Dispatcher 形成死锁。
- 大量图片仅加载缩略图并虚拟化显示；行情缓存有空间上限和可清理入口，清缓存不删除用户笔记和截图。

### 10.3 完成交付条件

- R01–R12 与 V01–V20 有对应实现、测试或实机证据；不能用“部分已有”跳过新增路径。
- Core、Infrastructure、新增 Application 用例测试及 Python 历史客户端测试通过，WPF Release 构建通过。
- 核验只读接口白名单，整个复盘/行情/回放链路没有订单写操作。
- 真实终端只做历史读取和既有持仓观察，不为验收主动制造订单；能用合成事件重现的风险路径离线验证。
- 发布包含新增依赖的 win-x64 包；使用该发布包验证导入、查询、写笔记、重启、回放和导出。
- 交付说明列出已知数据缺口与仍保留的净额账户边界；TODO 记录可核验结果后，才能将“方案已定义”改为“已实现”。

## 11. 实施结果与已知边界

R01–R12 已在同一工作区交付：账户复合身份和来源证据、复盘文档修订与精确需重审、统一筛选和队列、日历/日记、策略版本和执行评价、交易想法、资金与风险分析、行为证据及可采用/撤销的候选标签、改进目标、真实行情回放、机会记录、公开导出、完整备份恢复、数据质量下钻和缓存治理均已接入 Core、Application、Infrastructure 与 WPF。

2026-09-09 的最终自动化结果为 Core 128、Application 24、Infrastructure 49、Python 16 项测试全部通过，WPF Release 构建 0 warnings / 0 errors。真实 WeTrade 终端只读验证在一笔已完成的 XAUUSD.s 交易上取得 27 根 M1 bars 和开仓附近 160 条 ticks，两个请求均返回 `complete`；证据见 [review-live-history-verification-2026-09-09.json](artifacts/review-live-history-verification-2026-09-09.json)。静态扫描未发现 `order_send`、`OrderSend`、`TRADE_ACTION_DEAL`、`TRADE_ACTION_PENDING` 或持仓关闭/买卖调用。

仍保留两项诚实边界：完整交易投影只支持 MT5 对冲账户；历史行情只能展示终端实际保留的数据，缺失区间会降级而不会补造。本次会话的 Computer Use 只提供浏览器表面，未提供原生 WPF 窗口，因此不能在这里生成 100%/150%/200% 三档实窗截图；代码已启用 PerMonitorV2，关键页面有纵向滚动容器和键盘流程，最终多缩放视觉确认需在用户桌面完成。
